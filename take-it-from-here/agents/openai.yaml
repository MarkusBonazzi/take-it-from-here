interface:
  display_name: "Take It From Here"
  short_description: "Move context to a fresh session, then stop"
  default_prompt: "Use $take-it-from-here to move only the essential continuation context into a fresh session, then step aside."
policy:
  allow_implicit_invocation: false
