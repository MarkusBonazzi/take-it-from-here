interface:
  display_name: "Take It From Here"
  short_description: "Move to a fresh session and keep working"
  default_prompt: "Use $take-it-from-here to move the essential context into a fresh session and continue from the next action."
policy:
  allow_implicit_invocation: false
